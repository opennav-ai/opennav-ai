# CLI Build-Script Example

This example shows the package-script shape for using the OpenNav CLI after any
static site build.

Look at:

- [`package.json`](./package.json) for the `build` script shape

The CLI command behavior is not implemented yet, so this example is not part of
`npm run test:examples` until `opennav static` can run successfully.
