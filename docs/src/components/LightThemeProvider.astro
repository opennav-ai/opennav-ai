---
---

<script is:inline>
  window.StarlightThemeProvider = (() => {
    document.documentElement.dataset.theme = "light";

    try {
      localStorage.setItem("starlight-theme", "light");
    } catch {
      // Keep the docs light-only even when storage is unavailable.
    }

    return {
      updatePickers() {
        document.documentElement.dataset.theme = "light";
      },
    };
  })();
</script>
