# @opennav-ai/engine

Core OpenNav AI implementation for generating, indexing, validating, and exposing agent-ready website artifacts.

This package is used by the CLI and framework adapters. It is published so every public package can share one engine version during installation.
